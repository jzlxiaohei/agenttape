import { Shell } from "@/ui/Shell";
import { CasesPanel } from "@/ui/CasesPanel";

// Replay library page.
export function CasesPage() {
  return (
    <Shell>
      <CasesPanel />
    </Shell>
  );
}
