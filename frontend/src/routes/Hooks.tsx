import { Shell } from "@/ui/Shell";
import { HookEventsPanel } from "@/ui/HookEventsPanel";

// Hook capture settings page.
export function HooksPage() {
  return (
    <Shell>
      <HookEventsPanel />
    </Shell>
  );
}
